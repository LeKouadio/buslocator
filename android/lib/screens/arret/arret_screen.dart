import 'package:flutter/material.dart';

class ArretScreen extends StatelessWidget {
  const ArretScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Arret')),
    );
  }
}